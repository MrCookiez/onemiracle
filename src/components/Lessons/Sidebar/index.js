import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
    margin: auto;
    text-align: center;
`;

const codePharse = '<h1>TETS HEADER</h1>';

const SidebarLessons = () => {
    return (
        <Container>
            
        </Container>
    );
}

export default SidebarLessons;